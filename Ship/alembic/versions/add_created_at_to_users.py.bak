"""Add created_at column to users table

Revision ID: add_created_at_column
Revises: a7a36e6a79cd
Create Date: 2025-11-12 00:00:00.000000

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = 'add_created_at_column'
down_revision = 'a7a36e6a79cd'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # Add created_at column to users table with default value
    op.add_column('users', sa.Column('created_at', sa.DateTime(), nullable=False, server_default=sa.func.now()))


def downgrade() -> None:
    # Remove created_at column
    op.drop_column('users', 'created_at')
